# 자리바꾸기 사용 설명서

교실 학생 자리를 조건에 맞춰 자동으로 배정하고, 예쁘게 꾸며 인쇄까지 하는 데스크톱 앱입니다.
이 문서는 모든 기능을 탭 순서대로 설명합니다.

- 빠른 시작이 궁금하면 → [2. 빠른 시작](#2-빠른-시작)
- 특정 기능만 보려면 → 아래 목차에서 해당 탭으로

---

## 목차

1. [소개](#1-소개)
2. [빠른 시작](#2-빠른-시작)
3. [화면 구성](#3-화면-구성)
4. [학생 명단 탭](#4-학생-명단-탭)
5. [좌석 설정 탭](#5-좌석-설정-탭)
6. [제약 탭](#6-제약-탭)
7. [배정 탭](#7-배정-탭)
8. [기록 탭](#8-기록-탭)
9. [꾸미기 탭](#9-꾸미기-탭)
10. [정보 탭](#10-정보-탭)
11. [데이터 저장과 백업](#11-데이터-저장과-백업)
12. [자주 묻는 질문](#12-자주-묻는-질문)

---

## 1. 소개

- 학생 **명단**을 넣고 → 교실 **좌석 모양**을 정하고 → (선택) **제약 조건**을 걸고 →
  **배정** 버튼 한 번이면 자리표가 완성됩니다.
- **확정한 배치는 기록**에 쌓여, 다음 배정 때 같은 자리·같은 짝을 피하는 기준이 됩니다.
- 완성된 자리표는 **꾸미기 탭**에서 스킨·배경·캐릭터로 꾸며 **PNG로 출력**(인쇄)할 수 있습니다.
- 설치가 필요 없습니다. `자리바꾸기.exe` 하나만 받아 더블클릭하면 실행됩니다.

---

## 2. 빠른 시작

1. **학생 명단** 탭에서 학생을 입력합니다. (엑셀·클립보드 붙여넣기 가능)
2. **좌석 설정** 탭에서 분단·행·열을 정합니다. (예: 3분단 4행 2열)
3. **배정** 탭에서 짝/회피 옵션을 고른 뒤 **배정**을 누릅니다.
   - 마음에 들 때까지 다시 눌러 다른 결과를 뽑습니다.
   - 좋으면 **확정**을 눌러 기록에 저장합니다.
4. (선택) **꾸미기** 탭에서 스킨을 고르고 **출력(PNG)** → 저장 후 인쇄(Ctrl+P).

제약(고정·회피·짝 거리 등)이 필요 없다면 3·6번 탭은 건너뛰어도 됩니다.

---

## 3. 화면 구성

위쪽 탭으로 이동합니다.

- **학생 명단** — 학번·이름·성별 관리 (수동/엑셀/붙여넣기)
- **좌석 설정** — 교실 좌석의 *공간*을 정의 (분단·행·열, 빈자리, 남녀 자리)
- **제약** — 특정 학생/자리 규칙 (고정·회피·짝 거리·앞자리). PIN으로 잠금
- **배정** — 실제 자리 배정 + 확정 + 수동 교체
- **기록** — 확정된 배치 누적 (다음 배정의 회피 기준)
- **꾸미기** — 자리표 스킨·배경·셀 이미지 + 출력(PNG)
- **정보** — 데이터 저장 위치, 안내

> **개념 구분**: *좌석 설정*은 자리의 **공간**(분단·행·열·빈자리·남녀 구역)만 정합니다.
> *짝(누구와 앉느냐)*은 **배정 탭**에서 결정됩니다. 둘은 별개입니다.

---

## 4. 학생 명단 탭

학번 · 이름 · 성별(남/녀)을 관리합니다.

**입력 방법**
- **수동**: 행 추가 후 직접 입력. 성별은 드롭다운.
- **엑셀/CSV 업로드**: `.xlsx` 또는 `.csv` 파일 선택.
- **클립보드 붙여넣기**: 엑셀·표에서 복사한 내용을 그대로 붙여넣기 (탭·쉼표 자동 인식).

**성별 자동 인식**: `남 / 여 / 녀 / M / F / 1 / 2 / male / female` 등을 인식합니다.
인식이 안 되면 *미지정*으로 들어갑니다.

**엑셀/CSV 형식 예시**

열 순서는 **학번 · 이름 · 성별** 을 권장합니다. 첫 행 머리글은 있어도 없어도 됩니다.

```
학번   이름     성별
1     김민준    남
2     이서연    여
3     박도윤    남
```

📎 **예시 양식**: [`examples/학생명단_예시.xlsx`](examples/학생명단_예시.xlsx) 를 내려받아
내용만 바꿔 업로드하면 됩니다. 엑셀에서 위 표를 복사해 **클립보드 붙여넣기**도 됩니다.

**학번을 비우면?**
- 학번 칸을 비워도 됩니다. 이때는 **이름이 식별 기준**이 됩니다(배정·기록 회피 모두 정상 작동).
- **이름이 같은 학생**이 학번 없이 들어오면, 가져올 때 자동으로 **`홍길동(2)`, `홍길동(3)`** 처럼
  뒤에 번호를 붙여 구분합니다(원하면 명단에서 직접 고칠 수 있습니다). 가져오기 알림에도 표시됩니다.
- 같은 **학번**이 둘 이상이면(보통 입력 실수) 같은 사람으로 취급되므로 **경고**가 뜹니다 — 학번을 수정하세요.

> 명단은 자동 저장되어 다음 실행 때 복원됩니다.

---

## 5. 좌석 설정 탭

교실 좌석의 **모양(공간)** 을 정의합니다.

- **분단 · 행 · 열**: 좌석 격자를 정합니다.
  - **열 1 = 단독석**(짝 없음), **열 2 이상 = 같은 행 인접 2칸이 짝**.
  - 열이 3 이상이면 좌→우로 2칸씩 짝, 홀수로 남는 칸은 단독석.
- **빈자리(비움)**: 좌석을 클릭하면 *비움*으로 토글됩니다. 배정에서 제외됩니다.
- **남녀 자리 지정**: 특정 좌석을 *남자리/여자리* 구역으로 칠합니다(페인트 모드).
  - 이는 **공간 지정**입니다(예: 한 분단을 남/여 열로 나누기). *이성짝 규칙과는 다릅니다.*

> 여기서 정한 격자 크기는 제약·배정·꾸미기에 그대로 반영됩니다.

---

## 6. 제약 탭

특정 학생/자리에 대한 세부 규칙을 정합니다. **학생에게 보이면 안 되는 민감한 정보**라
**PIN(비밀번호)으로 잠글 수 있습니다.**

### 잠금
- 처음에 PIN을 설정하면 이후 탭을 열 때마다 PIN을 물어봅니다.
- 다른 탭으로 나가면 자동으로 다시 잠깁니다.
- **PIN을 잊었다면**: 복구 시 *제약 내용이 모두 초기화*되고 PIN이 재설정됩니다.

### 제약 종류 (우선순위 카드)
각 규칙은 카드로 표시되며, **오른쪽 손잡이(≡)를 드래그**해 우선순위를 바꿀 수 있습니다.
조건이 서로 충돌하면 **우선순위가 낮은 것부터 자동으로 완화**됩니다.

- **자리 고정** — 특정 학생을 특정 좌석(분단·행·열)에 고정
- **자리 회피** — 특정 학생이 특정 좌석을 피하게
- **가급적 가깝게** — 두 학생을 가까이 — ① 가까이 ② 반드시 짝
- **가급적 멀리** — 두 학생을 멀리 — ① 같은 짝만 금지 ② 팔방(주변) 금지 ③ 멀리
- **남녀 자리** — 좌석 설정에서 지정한 남/여 구역을 규칙으로 반영
- **앞자리** — 특정 학생을 앞쪽 N개 행에 배치

> 좌석 좌표 입력칸의 최댓값은 **좌석 설정의 격자 크기**를 자동으로 따릅니다.
> 거리(가깝게/멀리)는 분단 사이 간격 없이 **균일 격자**로 계산합니다.

---

## 7. 배정 탭

실제로 자리를 배정합니다.

### 배정 옵션 (제약이 아닌 *옵션*)
- **동성짝 / 이성짝**: 짝을 같은 성별 / 다른 성별로 (열 2 이상일 때만).
- **이전과 같은 자리 회피** / **이전과 같은 짝 회피**: 기록과 대조해 반복을 피함.

### 배정하기
- **배정**을 누를 때마다 *다른 후보*가 생성됩니다. 마음에 들 때까지 다시 누르세요.
- 좋으면 **확정** → 기록에 저장됩니다.
- 남/녀는 색으로 구분됩니다(남=파랑, 녀=분홍).

### 수동 교체
- 배정 결과에서 **좌석 두 개를 차례로 클릭**하면 두 학생이 서로 바뀝니다.

### 자동 완화 배너
- 조건을 모두 만족할 수 없으면 앱이 **우선순위가 낮은 제약부터 자동으로 완화**하고,
  무엇을 완화했는지 **노란 배너**로 알려줍니다. (배정은 항상 완성됩니다.)
  - 예: "1열(단독석)인데 반드시 짝" 처럼 모순되면 우선순위에 따라 한쪽을 풉니다.

---

## 8. 기록 탭

- **확정한 배치**가 시각·라벨·구성 요약과 함께 누적됩니다.
- 항목을 선택하면 좌석 **미리보기**가 보이고, **라벨 수정·삭제**가 가능합니다.
- **확정된 전체 기록**이 다음 배정의 *같은 자리/같은 짝 회피* 기준이 됩니다.
- 기록을 삭제하면 그만큼 회피 기준도 줄어듭니다.

---

## 9. 꾸미기 탭

확정한(또는 기록의) 자리표를 꾸며 **PNG로 출력**합니다. 상단 도구는 네 구역으로 나뉩니다.

### ① 불러오기
- **현재 배정**(배정 탭 결과) 또는 **기록**의 확정 배치 중 하나를 골라 미리보기에 띄웁니다.

### ② 스킨 (좌석표 색)
- 작은 색칩을 클릭해 스킨을 고릅니다. 9종: 기본·파스텔·칠판·하늘·노을·민트·라벤더·모노·숲.
- 스킨은 **출력 좌석표의 색**(배경·카드색·칠판·제목)만 바꿉니다. 앱 화면은 그대로입니다.

### ③ 커스텀 이미지
- **배경**: 좌석표 전체 배경에 깔 이미지를 선택합니다(PNG·JPG 등).
- **남자리 셀 / 여자리 셀**: 좌석 카드 배경을 커스텀 이미지로. 한쪽만 넣으면 나머지는 스킨 색.
- **✕** 버튼으로 각각 해제합니다.
- **셀 투명 배경**: 특이한 모양/투명 PNG 셀일 때 카드 테두리·둥근 모서리 없이 *이미지 모양 그대로* 표시.
  불투명 이미지(흰/단색 배경)는 **모서리 배경색을 자동으로 제거(누끼)** 합니다.

### ④ 출력
- **칠판 표시**: 끄면 칠판(앞) 표식을 그리지 않습니다(배경과 안 맞을 때).
- **교탁 기준**: 좌석을 좌우·상하 거울반전하고 칠판을 아래로 둡니다.
  교탁에 붙여 학생을 마주 봤을 때 위치가 맞습니다(글자는 똑바로).
- **출력(PNG)**: 저장 위치를 고르면 PNG로 저장하고 기본 뷰어로 엽니다 → 거기서 **Ctrl+P**로 인쇄.

> 스킨·교탁·칠판·이미지 설정은 저장되어 다음에도 유지됩니다.

---

## 10. 정보 탭

- 데이터 저장 폴더 위치와 각 파일 경로를 보여주고, **저장 폴더 열기** 버튼을 제공합니다.
- **꾸미기 이미지 안내**: 배경·셀 이미지는 앱에 *복사되지 않고 경로만 저장*됩니다(아래 참고).

---

## 11. 데이터 저장과 백업

명단·기록·설정은 아래 폴더에 저장됩니다.

- **Windows**: `%APPDATA%\SeatShuffler`
- **리눅스·맥**: `~/.config/SeatShuffler`

폴더 안의 파일:

- `roster.json` — 학생 명단
- `history.json` — 확정 기록
- `constraints.json` — 제약 · 좌석 설정 · 꾸미기 설정

> **꾸미기 이미지 주의**: 배경·셀 이미지는 *원본 파일의 경로만* 저장됩니다.
> 원본을 옮기거나 이름을 바꾸거나 삭제하면 그림이 사라지고 기본 색으로 돌아갑니다.
> 다른 PC로 옮길 때는 이미지 파일도 함께 옮기고 꾸미기 탭에서 다시 지정하세요.
> **고정된 폴더(예: 사진 폴더)** 에 두고 쓰면 안전합니다.

**백업**: 위 폴더의 `.json` 세 파일을 복사해 두면 명단·기록·설정이 보존됩니다.

---

## 12. 자주 묻는 질문

**Q. 설치가 필요한가요?**
아니요. `자리바꾸기.exe` 하나만 받아 더블클릭하면 됩니다(.NET 설치 불필요).

**Q. 배정이 매번 같게 나와요.**
**배정** 버튼을 누를 때마다 새 후보가 나옵니다. 여러 번 눌러 보세요. *확정*하면 기록에 남아
다음부터는 같은 자리·같은 짝을 피합니다.

**Q. 조건이 서로 충돌하면요?**
앱이 우선순위가 낮은 제약부터 자동으로 완화하고, 무엇을 풀었는지 배너로 알려줍니다.
배정은 항상 완성됩니다. 우선순위는 *제약 탭*에서 드래그로 바꿀 수 있습니다.

**Q. 제약 PIN을 잊었어요.**
복구할 수 있지만 *제약 내용이 모두 초기화*됩니다(보안상). 명단·기록은 그대로입니다.

**Q. 학번을 안 넣어도 되나요?**
네. 비우면 *이름*이 식별 기준이 되어 정상 작동합니다. **이름이 같은 학생**이 학번 없이 들어오면
가져올 때 자동으로 `홍길동(2)`처럼 번호를 붙여 구분하므로 섞이지 않습니다(원하면 직접 수정).

**Q. 성별 미지정 학생은 짝이 어떻게 되나요?**
*동성짝*에서는 누구와도 짝이 될 수 있고(와일드카드), *이성짝*에서는 엄격한 쌍에
부적격이라 완화를 유발할 수 있습니다.

**Q. 인쇄가 안 보여요.**
앱에는 인쇄 대화상자가 없습니다. *출력(PNG)* 으로 저장하면 기본 이미지 뷰어가 열리고,
거기서 **Ctrl+P** 로 인쇄하면 됩니다.

---

문의·개선 요청은 저장소 이슈로 남겨 주세요.
